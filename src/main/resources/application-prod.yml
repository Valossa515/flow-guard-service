spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

springdoc:
  swagger-ui:
    enabled: false

logging:
  level:
    root: WARN
    io.github.valossa515.flow_guard_service: INFO
